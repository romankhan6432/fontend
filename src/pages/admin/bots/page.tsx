import AdminBotsContent from "./content"

export const metadata = {
    title: "Bot Management | Admin Panel",
    description: "Manage all registered bots, credits, and balances",
}

export default function AdminBotsPage() {
    return <AdminBotsContent />
}
