import UploadModelContent from "./content"

export const metadata = {
    title: "Upload Model | Admin Panel",
    description: "Upload .pt model files to bot endpoints",
}

export default function UploadModelPage() {
    return <UploadModelContent />
}
